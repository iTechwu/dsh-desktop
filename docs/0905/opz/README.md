# Yootun-Agent 全量审计与优化

本目录记录 2026-09-04 的 Yootun-Agent 全量审计、插件测试、体验问题、修复、部署和回归证据。审计从三个前置任务全部完成后开始，计划持续到北京时间 07:30 之后完成 macOS 与 Windows 应用打包验证。

## 当前状态

- 前置任务：3/3 已完成。
- 仓库门禁：旧聚合 Web UI 的 7 个过期断言已修复；最新完整检查为 Market 268/268、Desktop 1108 通过/4 跳过，其余构建与验证全通过。
- 首轮界面冒烟：主界面、设置、插件市场、12 个侧栏业务入口和 Skill 管理已检查。
- 聊天执行：真实模型请求、只读与失败 Bash 工具调用、失败后续发、停止后续发、新会话隔离、完成态与进行中重启恢复均通过；简单冒烟请求输入约 78-80K token，列为上下文成本问题。
- 业务面板互斥与模态焦点：11 个插件契约测试、背景隔离、弹窗内聚焦、Escape 关闭和触发点归焦均已通过真实 Electron 回归。
- 消息插件：真实 `desktop` Profile 已从 `dsh-im@2.3.0` 升级到 `4.9.1`；安装阶段机器人配置与工作区映射哈希不变，首次启动将工作区文档迁移为 v2 访问策略且保留原绑定。飞书为 1/1 在线，连接检查成功发送测试消息，连续观察超过两个旧重试周期没有新增 `host.describe` 403；当前保留 2 个升级前快照和 1 个 4.9.1 健康快照。
- Skill 管理：确认 112 行无界渲染导致 Electron/macOS 原生 AX 裁剪；视觉与键盘可用，3 行诊断可恢复完整 AX，上游插件需虚拟化或分页。
- 详情面板：折叠态已使用 `inert` 与 `aria-hidden` 隔离；真实包验证关闭后详情内容退出 macOS AX，工具记录仍可重新展开并完整访问。
- 企业驾驶舱：刷新、昨日/近 7 天/近 30 天范围及 4/4 数据源已回归；修复真实租户下 GEO 趋势因未注入时钟而降级的问题，插件提交 `b94319b` 已推送。
- 招聘工作台：真实刷新、总览/岗位/人才库/知识库/BOSS 同步页面、空输入校验和同步预览均通过；没有触发外部登录或写入，插件 5/5、桌面招聘测试 31/31 通过。
- 插件门禁：招聘、销售、供应链、内容指挥、统一审批的已有 Host 路由测试已纳入默认 `npm test`；进一步修复 7 个业务插件缺少 Harness `AbortSignal` 的工具调用契约，并覆盖工具异常回退，七包最新 gate 共 37/37 通过，提交 `066ae47`、`8bd495c` 已推送。
- 线索发现：真实“已存候选”从 `lead_discovery_request_failed` 恢复为 0 条候选的诚实空态；供应链、内容指挥和统一审批在同一重包中回归通过，06:49 后无新增业务插件失败日志。
- 改装检索：修复 Host 模糊匹配到自身工具后无限递归的问题；真实查询由窗口失去响应、主进程约 100% CPU/1.2 GiB 内存恢复为 2 秒内诚实降级。公共 MCP 当前没有声明 `query` 参数的 Custom Car 工具，待服务契约补齐后再验收真实结果。
- 其余插件写操作、窄窗口和完整键盘回归：待按测试矩阵继续执行。
- 部署：本轮尚无需要部署的修复。
- 应用打包：等待北京时间 07:30 后执行。

## 文档索引

- [审计报告](audit-report.md)：问题、证据、优先级、归属和修复状态。
- [插件与工作区测试用例](test-cases.md)：覆盖插件、工作区、聊天、设置、打包和真实环境。
- [体验问题](ux-issues.md)：从用户工作流观察到的界面与交互问题。
- [回归记录](regression-log.md)：每一轮命令、界面、部署和结果证据。

## 约束

- 不打印、记录或提交任何凭据值。
- 模拟数据必须带审计前缀、可追踪且不得覆盖或删除既有生产数据。
- 需要部署时只走 `183.215.43.243` 上既有 Jenkins 与仓库契约，不直接修改 CI 工作副本。
- 每批修改通过对应 gate 后，使用中文 Conventional Commits 提交并推送 `origin`。
