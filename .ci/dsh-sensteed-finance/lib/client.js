window.__ModuleLoader__.load({
  id: "@dofe/dsh-sensteed-finance",
  factory: (require) => { var module = { exports: {} }; var exports = module.exports;
    globalThis.__DSH_PLUGIN_BRAND__ = {"tenant":"yootun","name":"Yootun-Agent","company":"优惠豚","eyebrow":"YOOTUN AGENT","knowledgeEyebrow":"YOOTUN KNOWLEDGE","help":"获取 model_api_key，请联系优惠豚 AI 部小伙伴：19996936963","helpEn":"For model_api_key, contact the Yootun AI team at 19996936963.","logoDataUrl":"data:image/png;base64,"};
    const React = require('react')
    const PLUGIN_BRAND = globalThis.__DSH_PLUGIN_BRAND__ || { tenant: 'sensteed', company: '山子' }
    const REQUEST_TIMEOUT_MS = 30000
    const { createElement: h, useEffect, useState, useRef, useSyncExternalStore } = React
    const {
      IconAlarmClockOutline16,
      IconCheckOutline16,
      IconCloseOutline16,
      IconDataOutline16,
      IconDatabaseOutline16,
      IconGoalOutline16,
      IconLoadingOutline16,
      IconRefreshOutline16,
      IconSparkle16,
      IconWarningOutline16,
      Tooltip,
    } = require('@deepseek-ai/dsh-client-ui-primitives')

    const NS = 'dofe.sensteed-finance'
    const OVERLAY_ID = '@dofe/dsh-sensteed-finance'
    const OVERLAY_EVENT = 'dofe:yootun-overlay:open'
    const BASE = '/api/desktop/sensteed/finance'
    const TABS = [['overview', 'overview'], ['budget', 'budget'], ['ledger', 'ledger'], ['cash', 'cash'], ['alerts', 'alerts'], ['governance', 'governance'], ['entry', 'entry'], ['analyze', 'analyze']]
    const EXPENSE_TYPES = [['PROJECT_OPERATION', 'expenseProject'], ['FIXED_OPERATION', 'expenseFixed'], ['MODEL_SPECIFIC', 'expenseModel'], ['NON_PROJECT', 'expenseNonProject']]
    const PLAN_TYPES = [['PURCHASE', 'planPurchase'], ['OTHER', 'planOther'], ['NEW_RETAIL', 'planNewRetail'], ['PR_PAYMENT', 'planPrPayment'], ['DIRECT_PARTS', 'planDirectParts']]
    const LEDGER_LIMIT = 100
    const BUDGET_LINE_LIMIT = 200

    const ANALYSIS_ENTRIES = [
      { id: 'risk', icon: 'warning', prompt: (year, org) => `请对${org || '全部主体'} ${year} 年度财务状况做一次风险预判：先调用 sensteed_finance_bootstrap 获取租户上下文，再用 mcp__finance__finance_analysis_brief 建立口径基线，然后下钻 mcp__finance__finance_get_alerts 与 mcp__finance__finance_get_budget_summary。按预算超支、执行偏慢、资金缺口、数据质量四类逐项给出：影响金额（元）、涉及主体与期间、严重级别、置信度、建议动作。结论先行，财务口吻。` },
      { id: 'cost', icon: 'layers', prompt: (year, org) => `请分析${org || '全部主体'} ${year} 年成本结构：调用 mcp__finance__finance_analysis_brief 与 mcp__finance__finance_get_budget_summary，按三类费用（项目运营/固定运营/模型专项）与主体维度拆解预算与实际付款占比，识别占比异常漂移与单点集中度风险，给出成本优化建议与下季度成本预判（声明外推方法与假设）。金额单位元，财务口吻。` },
      { id: 'cash', icon: 'goal', prompt: (year, org) => `请对${org || '全部主体'} ${year} 年资金链做压力评估：调用 mcp__finance__finance_get_cash_summary 与 mcp__finance__finance_get_payment_plans，梳理各月期初/收入/支出/预计余额，识别预计余额为负或骤降的月份，结合付款计划给出未来 1-3 个月的资金缺口预判与头寸安排建议。金额单位元，财务口吻。` },
      { id: 'budget', icon: 'data', prompt: (year, org) => `请复盘${org || '全部主体'} ${year} 年预算执行：调用 mcp__finance__finance_get_budget_summary 与 mcp__finance__finance_get_budget_lines，计算各主体的执行率（(已打PR+预计PR)/预算）与实际支付率（实际付款/预算），输出执行率排名、偏离原因归类（录入滞后/计划变更/预算虚高），并给出预算调整建议。金额单位元，财务口吻。` },
      { id: 'revenue', icon: 'sparkle', prompt: (year, org) => `请分析${org || '全部主体'} ${year} 年收入达成与差额：调用 mcp__finance__finance_get_revenue_plans 与 mcp__finance__finance_get_cash_summary，逐项比对计划收入与实际到账，归因未达成项（延期/缩水/未回填），评估对全年资金平衡的影响并给出催收与计划修正建议。金额单位元，财务口吻。` },
      { id: 'quality', icon: 'database', prompt: `请评估财务数据中心当前的数据质量对分析口径的影响：调用 mcp__finance__finance_get_data_quality、mcp__finance__finance_get_import_batches 与 mcp__finance__finance_get_alerts（severity=DATA_QUALITY 或按警情类型判断）。列出问题清单、涉及数据集与数量级，说明哪些分析结论会因此失真，并给出补数与修复的优先级建议。财务口吻。` },
      { id: 'forecast', icon: 'clock', prompt: (year, org) => `请对${org || '全部主体'} ${year} 年剩余期间做成本与资金预判：调用 mcp__finance__finance_analysis_brief 后，按近 3 个月实际付款均值与已打 PR 未付款部分，外推未来支出；结合收入计划到账节奏，给出月末资金余额区间预判（声明方法与假设），并标注高置信/低置信结论。金额单位元，财务口吻。` },
    ]

    const copy = {
      zh: {
        open: '财务看板', title: '财务专业看板', subtitle: '预算 · 台账 · 资金 · 预警 · 录入 · 深度分析', close: '关闭财务看板', refresh: '刷新数据',
        overview: '总览', budget: '预算', ledger: '台账与计划', cash: '资金', alerts: '预警', governance: '数据治理', entry: '数据录入', analyze: '深度分析',
        year: '年度', org: '主体', allOrgs: '全部主体', loading: '正在加载财务数据...', retry: '重新加载', loadError: '数据加载失败', refreshedAt: '更新于', unitWan: '万元', unitYuan: '元',
        kpiBudget: '预算总额', kpiPrSubmitted: '已打 PR', kpiPrEstimated: '预计 PR', kpiPaid: '实际付款', kpiAlerts: '未关闭预警', kpiExecRate: '预算执行率',
        trendTitle: '月度趋势（已打 PR / 实际付款）', month: '月份', amount: '金额', empty: '暂无数据', orgSummary: '分主体汇总', alertSummary: '预警摘要',
        budgetSummaryTitle: '预算汇总（主体 × 月度 × 三类费用）', budgetLinesTitle: '预算明细行', expenseType: '费用类别', department: '部门', costItem: '费用科目',
        prTitle: '付款申请（PR）台账', paymentPlansTitle: '付款计划', revenuePlansTitle: '收入计划', planType: '计划类型', planned: '计划金额', actual: '实际金额', variance: '差额', remaining: '剩余', status: '状态', backfill: '回填', backfillActual: '回填实际值', backfillAmount: '实际金额（元）', backfillDate: '实际日期（YYYY-MM-DD，可空）', submit: '提交', cancel: '取消', submitted: '已提交', itemName: '收入项目', payer: '付款方', incomeType: '收入类型', recordType: 'PR 类型', prSubmitted: '已打 PR', prEstimated: '预计 PR',
        analysis_risk: '月度风险预判', analysis_risk_desc: '按预算超支/执行偏慢/资金缺口/数据质量四类输出风险清单，附影响金额与建议动作。',
        analysis_cost: '成本结构分析', analysis_cost_desc: '三类费用与主体维度拆解成本占比，识别集中度风险并预判下季度成本。',
        analysis_cash: '资金流压力评估', analysis_cash_desc: '梳理各月资金余缺，预判未来 1-3 个月资金缺口与头寸安排。',
        analysis_budget: '预算执行复盘', analysis_budget_desc: '各主体执行率排名与偏离归因，输出预算调整建议。',
        analysis_revenue: '收入达成分析', analysis_revenue_desc: '计划 vs 实际到账逐项比对，归因差额并评估对资金平衡的影响。',
        analysis_quality: '数据质量影响评估', analysis_quality_desc: '列出质量问题清单，说明哪些分析结论会失真及补数优先级。',
        analysis_forecast: '成本与资金预判', analysis_forecast_desc: '按近 3 个月外推未来支出与月末资金余额区间，标注置信度。',
        alertsTitle: '预警清单', runEngine: '触发预警引擎', running: '引擎执行中', runDone: '已触发：新建 {created} · 更新 {updated} · 关闭 {resolved}', severity: '级别', alertType: '类型', title: '标题', detail: '说明', firstSeen: '首次发现', lastSeen: '最近出现',
        sevCritical: '重大', sevWarn: '关注', sevInfo: '提示', statusOpen: '待处理', statusAll: '全部状态', total: '合计',
        qualityTitle: '数据质量清单', batchesTitle: '导入批次', issue: '问题', dataset: '数据集', count: '数量', sourceFile: '来源文件', rows: '行数', successRows: '成功', failedRows: '失败', batchStatus: '状态', startedAt: '开始时间',
        entryTitle: '财务数据录入', entryPaymentPlan: '录入付款计划', entryRevenuePlan: '录入收入计划', entryBudgetLine: '录入预算行', description: '付款描述', payee: '收款方', planMonth: '计划月份', quarter: '季度', note: '备注', entryHint: '录入直接写入财务数据中心；提交前请核对金额与期间。提交后建议在预警页触发一次规则引擎。',
        analyzeTitle: '深度分析入口', analyzeHint: '以下入口会把预制指令发送到当前会话，由 Agent 调用财务数据中心 MCP（mcp__finance__*）完成深度分析，产出财务口吻的风险与成本预判。', send: '开始分析', sent: '已发送到会话', sendFail: '发送失败，指令已复制到剪贴板', analyzingNote: '分析会读取真实账面数据；录入/回填类操作 Agent 会先与你确认再执行。',
        showFirst: '仅显示前 {n} 条，可按年度/主体缩小范围', partialLoad: '部分数据块加载失败，当前展示已获取部分；刷新可重试。',
        unknown: '未知', dash: '—',
      },
      en: {
        open: 'Finance', title: 'Finance Workspace', subtitle: 'Budget · Ledger · Cash · Alerts · Entry · Deep analysis', close: 'Close finance workspace', refresh: 'Refresh',
        overview: 'Overview', budget: 'Budget', ledger: 'Ledger & plans', cash: 'Cash', alerts: 'Alerts', governance: 'Governance', entry: 'Data entry', analyze: 'Deep analysis',
        year: 'Year', org: 'Org', allOrgs: 'All orgs', loading: 'Loading finance data...', retry: 'Retry', loadError: 'Failed to load', refreshedAt: 'Updated', unitWan: '10k', unitYuan: 'CNY',
        kpiBudget: 'Budget', kpiPrSubmitted: 'PR submitted', kpiPrEstimated: 'PR estimated', kpiPaid: 'Paid', kpiAlerts: 'Open alerts', kpiExecRate: 'Execution rate',
        trendTitle: 'Monthly trend (PR submitted / paid)', month: 'Month', amount: 'Amount', empty: 'No data', orgSummary: 'By org', alertSummary: 'Alert summary',
        budgetSummaryTitle: 'Budget summary (org × month × type)', budgetLinesTitle: 'Budget lines', expenseType: 'Expense type', department: 'Department', costItem: 'Cost item',
        prTitle: 'Payment requests (PR)', paymentPlansTitle: 'Payment plans', revenuePlansTitle: 'Revenue plans', planType: 'Plan type', planned: 'Planned', actual: 'Actual', variance: 'Variance', remaining: 'Remaining', status: 'Status', backfill: 'Backfill', backfillActual: 'Backfill actuals', backfillAmount: 'Actual amount (CNY)', backfillDate: 'Actual date (YYYY-MM-DD, optional)', submit: 'Submit', cancel: 'Cancel', submitted: 'Submitted', itemName: 'Item', payer: 'Payer', incomeType: 'Income type', recordType: 'PR type', prSubmitted: 'Submitted', prEstimated: 'Estimated',
        analysis_risk: 'Risk forecast', analysis_risk_desc: 'Risk list across overrun / slow execution / cash gap / data quality, with impact and actions.',
        analysis_cost: 'Cost structure', analysis_cost_desc: 'Cost mix by expense type and org, concentration risks, next-quarter cost forecast.',
        analysis_cash: 'Cash pressure', analysis_cash_desc: 'Monthly cash surplus/deficit and 1-3 month gap forecast.',
        analysis_budget: 'Budget review', analysis_budget_desc: 'Execution-rate ranking by org with deviation attribution and adjustment advice.',
        analysis_revenue: 'Revenue attainment', analysis_revenue_desc: 'Plan vs actual receipts with variance attribution.',
        analysis_quality: 'Data quality impact', analysis_quality_desc: 'Issue list, which conclusions are distorted, and remediation priorities.',
        analysis_forecast: 'Cost & cash forecast', analysis_forecast_desc: '3-month-average extrapolation with confidence labels.',
        alertsTitle: 'Alerts', runEngine: 'Run alert engine', running: 'Running...', runDone: 'Done: created {created} · updated {updated} · resolved {resolved}', severity: 'Severity', alertType: 'Type', title: 'Title', detail: 'Detail', firstSeen: 'First seen', lastSeen: 'Last seen',
        sevCritical: 'Critical', sevWarn: 'Warn', sevInfo: 'Info', statusOpen: 'Open', statusAll: 'All', total: 'Total',
        qualityTitle: 'Data quality', batchesTitle: 'Import batches', issue: 'Issue', dataset: 'Dataset', count: 'Count', sourceFile: 'Source file', rows: 'Rows', successRows: 'OK', failedRows: 'Failed', batchStatus: 'Status', startedAt: 'Started',
        entryTitle: 'Finance data entry', entryPaymentPlan: 'Payment plan', entryRevenuePlan: 'Revenue plan', entryBudgetLine: 'Budget line', description: 'Description', payee: 'Payee', planMonth: 'Plan month', quarter: 'Quarter', note: 'Note', entryHint: 'Entries write to the finance data center directly. Verify amounts and periods before submitting; run the alert engine afterwards.',
        analyzeTitle: 'Deep analysis entries', analyzeHint: 'Each entry sends a prepared instruction to the current conversation; the agent calls finance MCP tools (mcp__finance__*) and returns CFO-tone risk & cost forecasts.', send: 'Analyze', sent: 'Sent to conversation', sendFail: 'Send failed; copied to clipboard', analyzingNote: 'Analysis reads real ledger data; write actions always require your confirmation first.',
        showFirst: 'Showing first {n} rows; narrow by year or org', partialLoad: 'Some blocks failed to load; showing what succeeded. Refresh to retry.',
        unknown: 'Unknown', dash: '—',
      },
    }

    let opened = false
    let lastTrigger = null
    const listeners = new Set()
    const emit = () => listeners.forEach(listener => listener())
    const setOpened = value => { opened = value; emit() }
    const openOverlay = event => { lastTrigger = event?.currentTarget || document.activeElement; window.dispatchEvent(new CustomEvent(OVERLAY_EVENT, { detail: { id: OVERLAY_ID } })); setOpened(true) }
    const closeOverlay = () => { setOpened(false); requestAnimationFrame(() => lastTrigger?.focus?.()) }
    const closeOtherOverlay = event => { if (event.detail?.id !== OVERLAY_ID) setOpened(false) }
    const subscribe = listener => { listeners.add(listener); return () => listeners.delete(listener) }
    const snapshot = () => opened

    async function api(path, options) {
      const response = await fetch(`${BASE}${path}`, { credentials: 'same-origin', redirect: 'error', headers: { Accept: 'application/json', ...(options?.body ? { 'Content-Type': 'application/json' } : {}) }, signal: AbortSignal.timeout(REQUEST_TIMEOUT_MS), ...options })
      const body = await response.json().catch(() => null)
      if (!response.ok || !body || body.ok === false) throw new Error(body?.error || `http_${response.status}`)
      return body
    }

    function finite(value) { const parsed = Number(value); return value !== null && value !== undefined && value !== '' && Number.isFinite(parsed) ? parsed : null }
    function formatNumber(value) { const parsed = finite(value); return parsed === null ? null : new Intl.NumberFormat(undefined, { maximumFractionDigits: 2 }).format(parsed) }
    function wan(value) { const parsed = finite(value); if (parsed === null) return null; if (Math.abs(parsed) >= 100000000) return `${(parsed / 100000000).toFixed(2)} 亿`; if (Math.abs(parsed) >= 10000) return `${(parsed / 10000).toFixed(2)} 万`; return formatNumber(parsed) }
    function ratio(part, total) { const p = finite(part), t = finite(total); if (p === null || !t) return null; return `${Math.round((p / t) * 100)}%` }
    function shortDate(value) { if (!value) return null; return String(value).slice(0, 10) }
    function yearOptions() { const year = new Date().getFullYear(); return [year - 1, year, year + 1].map(value => [String(value), String(value)]) }

    function LimitNote({ rows, limit, t }) {
      return rows?.length >= limit ? h('p', { className: 'sf-panel-note' }, t('showFirst').replace('{n}', String(limit))) : null
    }

    function Glyph({ name, size = 16, className }) {
      const icons = { check: IconCheckOutline16, warning: IconWarningOutline16, close: IconCloseOutline16, refresh: IconRefreshOutline16, data: IconDataOutline16, sparkle: IconSparkle16, goal: IconGoalOutline16, clock: IconAlarmClockOutline16, database: IconDatabaseOutline16, loading: IconLoadingOutline16 }
      const Icon = icons[name]
      return Icon ? h(Icon, { size, className }) : null
    }

    function Metric({ icon, label, value, hint, tone }) {
      return h('article', { className: `sf-metric${tone ? ` sf-tone-${tone}` : ''}` }, h('div', { className: 'sf-metric-label' }, h(Glyph, { name: icon, size: 14 }), h('span', null, label)), h('strong', null, value ?? '—'), hint ? h('small', null, hint) : null)
    }

    function Table({ columns, rows, empty }) {
      if (!rows?.length) return h('div', { className: 'sf-empty' }, empty)
      return h('div', { className: 'sf-table-scroll' }, h('table', { className: 'sf-table' },
        h('thead', null, h('tr', null, ...columns.map((col, index) => h('th', { key: index }, col.label)))),
        h('tbody', null, ...rows.map((row, rowIndex) => h('tr', { key: row.key ?? rowIndex }, ...columns.map((col, colIndex) => h('td', { key: colIndex, className: col.num ? 'sf-num' : '' }, col.render ? col.render(row) : row[col.key] ?? '—')))))))
    }

    function Bar({ value, tone }) {
      const parsed = finite(value)
      return h('div', { className: 'sf-bar-track' }, parsed === null ? null : h('div', { className: `sf-bar-fill${tone ? ` sf-bar-${tone}` : ''}`, style: { width: `${Math.max(0, Math.min(1, parsed)) * 100}%` } }))
    }

    function TrendChart({ months, series, t }) {
      const points = (months || []).map(month => ({
        month,
        prSubmitted: finite(series?.prSubmitted?.[month]),
        paid: finite(series?.paid?.[month]),
      }))
      if (!points.length || points.every(point => point.prSubmitted === null && point.paid === null)) return h('div', { className: 'sf-empty' }, t('empty'))
      const width = 720, height = 200, left = 48, right = 12, top = 14, bottom = 28
      const max = Math.max(...points.flatMap(point => [point.prSubmitted ?? 0, point.paid ?? 0]), 1)
      const plotBottom = height - bottom
      const bandWidth = (width - left - right) / points.length
      const barWidth = Math.max(4, Math.min(16, bandWidth * 0.3))
      const y = value => top + (plotBottom - top) * (1 - value / max)
      const labelEvery = Math.max(1, Math.ceil(points.length / 12))
      return h('div', { className: 'sf-chart-wrap' },
        h('div', { className: 'sf-chart-legend' }, h('span', null, h('i', { className: 'sf-legend-a' }), t('kpiPrSubmitted')), h('span', null, h('i', { className: 'sf-legend-b' }), t('kpiPaid'))),
        h('svg', { className: 'sf-chart', viewBox: `0 0 ${width} ${height}`, role: 'img', 'aria-label': t('trendTitle') },
          ...[0, 0.5, 1].map((step, index) => h('line', { key: index, x1: left, x2: width - right, y1: y(max * step), y2: y(max * step), className: 'sf-grid-line' })),
          ...points.map((point, index) => {
            const center = left + bandWidth * index + bandWidth / 2
            return h('g', { key: point.month },
              point.prSubmitted === null ? null : h('rect', { x: center - barWidth - 1, y: y(point.prSubmitted), width: barWidth, height: Math.max(0, plotBottom - y(point.prSubmitted)), className: 'sf-bar-a' }),
              point.paid === null ? null : h('rect', { x: center + 1, y: y(point.paid), width: barWidth, height: Math.max(0, plotBottom - y(point.paid)), className: 'sf-bar-b' }),
              index % labelEvery === 0 || index === points.length - 1 ? h('text', { x: center, y: height - 8, className: 'sf-chart-label', textAnchor: 'middle' }, point.month) : null)
          })))
    }

    function SeverityBadge({ severity, t }) {
      const key = severity === 'CRITICAL' ? 'sevCritical' : severity === 'WARN' ? 'sevWarn' : 'sevInfo'
      return h('span', { className: `sf-badge sf-badge-${severity === 'CRITICAL' ? 'critical' : severity === 'WARN' ? 'warn' : 'info'}` }, t(key))
    }

    function FormRow({ label, children }) {
      return h('label', { className: 'sf-form-row' }, h('span', null, label), children)
    }

    function Field(props) {
      const { value, onChange, ...rest } = props
      return h('input', { ...rest, value: value ?? '', onChange: event => onChange(event.target.value) })
    }

    function Select({ value, onChange, options, placeholder }) {
      return h('select', { value: value ?? '', onChange: event => onChange(event.target.value) },
        placeholder ? h('option', { value: '' }, placeholder) : null,
        ...options.map(([id, label]) => h('option', { key: id, value: id }, label)))
    }

    // ---- 页面：总览 ----

    function Overview({ brief, t }) {
      if (!brief) return h('div', { className: 'sf-empty' }, t('empty'))
      const data = brief.data || {}
      const overview = data.overview || {}
      const summary = data.alertsSummary || {}
      // 契约：overview.metrics 为扁平对象（FinanceMetricSchema），trend 点字段为 *Amount
      const m = overview.metrics || {}
      const kpi = { budget: m.budgetAmount, prSubmitted: m.prSubmittedAmount, prEstimated: m.prEstimatedAmount, paid: m.paidAmount }
      const trend = overview.trend || []
      const months = trend.map(point => String(point.month).padStart(2, '0'))
      const trendSeries = { prSubmitted: {}, paid: {} }
      for (const point of trend) {
        const key = String(point.month).padStart(2, '0')
        trendSeries.prSubmitted[key] = point.prSubmittedAmount
        trendSeries.paid[key] = point.paidAmount
      }
      return h('div', { className: 'sf-view' },
        h('div', { className: 'sf-metrics' },
          h(Metric, { icon: 'data', label: t('kpiBudget'), value: wan(kpi.budget), hint: t('unitYuan') }),
          h(Metric, { icon: 'layers', label: t('kpiPrSubmitted'), value: wan(kpi.prSubmitted), hint: `${t('kpiExecRate')} ${ratio(finite(kpi.prSubmitted) + finite(kpi.prEstimated), kpi.budget) || '—'}` }),
          h(Metric, { icon: 'clock', label: t('kpiPrEstimated'), value: wan(kpi.prEstimated) }),
          h(Metric, { icon: 'goal', label: t('kpiPaid'), value: wan(kpi.paid), hint: `${t('actual')}/${t('kpiBudget')} ${ratio(kpi.paid, kpi.budget) || '—'}` }),
          h(Metric, { icon: 'warning', label: t('kpiAlerts'), value: summary.total ?? 0, hint: `${t('sevCritical')} ${summary.critical ?? 0} · ${t('sevWarn')} ${summary.warn ?? 0}`, tone: summary.critical > 0 ? 'critical' : summary.warn > 0 ? 'warn' : 'ok' })),
        h('section', { className: 'sf-panel' }, h('h2', null, t('trendTitle')), h(TrendChart, { months, series: trendSeries, t })),
        h('div', { className: 'sf-two-col' },
          h('section', { className: 'sf-panel' }, h('h2', null, t('orgSummary')), h(Table, {
            columns: [
              { label: t('org'), key: 'orgName' },
              { label: t('kpiBudget'), render: row => wan(row.budgetAmount) ?? '—', num: true },
              { label: t('kpiPrSubmitted'), render: row => wan(row.prSubmittedAmount) ?? '—', num: true },
              { label: t('kpiPaid'), render: row => wan(row.paidAmount) ?? '—', num: true },
              { label: t('kpiExecRate'), render: row => ratio(finite(row.prSubmittedAmount) + finite(row.prEstimatedAmount), row.budgetAmount) ?? '—', num: true },
            ],
            rows: overview.byOrg || [],
            empty: t('empty'),
          })),
          h('section', { className: 'sf-panel' }, h('h2', null, t('alertSummary')), h(Table, {
            columns: [
              { label: t('severity'), render: row => h(SeverityBadge, { severity: row.severity, t }) },
              { label: t('title'), key: 'title' },
              { label: t('org'), key: 'orgName' },
            ],
            rows: summary.top || [],
            empty: t('empty'),
          }))))
    }

    // ---- 页面：预算 ----

    function Budget({ budget, year, t }) {
      // 契约：BudgetSummaryResponse = { list, totals }
      const summaryRows = budget?.summary?.list || []
      const totals = budget?.summary?.totals
      const summary = totals ? [...summaryRows, { ...totals, orgName: t('total'), month: null, expenseType: null }] : summaryRows
      const lines = budget?.lines?.list || []
      return h('div', { className: 'sf-view' },
        h('section', { className: 'sf-panel' }, h('h2', null, `${t('budgetSummaryTitle')} · ${year}`), h(Table, {
          columns: [
            { label: t('org'), key: 'orgName' },
            { label: t('month'), render: row => row.month ?? '—', num: true },
            { label: t('expenseType'), key: 'expenseType' },
            { label: t('kpiBudget'), render: row => wan(row.budgetAmount) ?? '—', num: true },
            { label: t('kpiPrSubmitted'), render: row => wan(row.prSubmittedAmount) ?? '—', num: true },
            { label: t('kpiPrEstimated'), render: row => wan(row.prEstimatedAmount) ?? '—', num: true },
            { label: t('kpiPaid'), render: row => wan(row.paidAmount) ?? '—', num: true },
            { label: t('kpiExecRate'), render: row => ratio(finite(row.prSubmittedAmount) + finite(row.prEstimatedAmount), row.budgetAmount) ?? '—', num: true },
          ],
          rows: summary,
          empty: t('empty'),
        })),
        h('section', { className: 'sf-panel' }, h('h2', null, t('budgetLinesTitle')), h(Table, {
          columns: [
            { label: t('org'), key: 'orgName' },
            { label: t('department'), key: 'departmentName' },
            { label: t('costItem'), key: 'costItemName' },
            { label: t('expenseType'), key: 'expenseType' },
            { label: t('month'), render: row => row.month ?? '—', num: true },
            { label: t('kpiBudget'), render: row => wan(row.budgetAmount) ?? '—', num: true },
          ],
          rows: lines,
          empty: t('empty'),
        }), h(LimitNote, { rows: lines, limit: BUDGET_LINE_LIMIT, t })))
    }

    // ---- 页面：台账与计划 ----

    function PlansTable({ title, rows, columns, limit, onBackfill, backfillId, onBackfillSubmit, onBackfillCancel, t }) {
      const allColumns = [...columns]
      if (onBackfill) allColumns.push({ label: '', render: row => h('button', { type: 'button', className: 'sf-link', onClick: () => onBackfill(row.id) }, t('backfill')) })
      return h('section', { className: 'sf-panel' }, h('h2', null, title),
        backfillId ? h('form', { className: 'sf-inline-form', onSubmit: event => { event.preventDefault(); onBackfillSubmit(new FormData(event.currentTarget)) } },
          h(FormRow, { label: t('backfillAmount') }, h(Field, { name: 'actualAmount', type: 'number', min: '0', step: '0.01', required: true })),
          h(FormRow, { label: t('backfillDate') }, h(Field, { name: 'actualPaidAt', type: 'date' })),
          h('div', { className: 'sf-form-actions' }, h('button', { type: 'submit', className: 'sf-primary' }, t('submit')), h('button', { type: 'button', onClick: onBackfillCancel }, t('cancel')))) : null,
        h(Table, { columns: allColumns, rows, empty: t('empty') }), h(LimitNote, { rows, limit, t }))
    }

    function Ledger({ ledger, t }) {
      const [backfill, setBackfill] = useState(null)
      const [busy, setBusy] = useState(false)
      const plans = ledger?.paymentPlans?.list || []
      const revenues = ledger?.revenuePlans?.list || []
      const submitBackfill = async formData => {
        setBusy(true)
        try {
          const payload = { actualAmount: Number(formData.get('actualAmount')) }
          const date = formData.get('actualPaidAt')
          if (date) payload.actualPaidAt = new Date(`${date}T00:00:00`).toISOString()
          const path = backfill.kind === 'revenue' ? `/revenue-plans/${backfill.id}/actuals` : `/payment-plans/${backfill.id}/actuals`
          await api(path, { method: 'POST', body: JSON.stringify(payload) })
          setBackfill(null)
          window.dispatchEvent(new CustomEvent('sf:refresh'))
        } finally { setBusy(false) }
      }
      return h('div', { className: 'sf-view' },
        h(PlansTable, {
          title: t('paymentPlansTitle'), rows: plans, t, limit: LEDGER_LIMIT,
          onBackfill: id => setBackfill({ id, kind: 'payment' }), backfillId: backfill?.kind === 'payment' ? backfill.id : null,
          onBackfillSubmit: submitBackfill, onBackfillCancel: () => setBackfill(null),
          columns: [
            { label: t('org'), key: 'orgName' },
            { label: t('planType'), key: 'planType' },
            { label: t('description'), key: 'description' },
            { label: t('planMonth'), render: row => row.planMonth ?? '—', num: true },
            { label: t('planned'), render: row => wan(row.plannedAmount) ?? '—', num: true },
            { label: t('actual'), render: row => wan(row.actualAmount) ?? '—', num: true },
            { label: t('remaining'), render: row => wan(row.remainingAmount) ?? '—', num: true },
            { label: t('status'), key: 'status' },
          ],
        }),
        h(PlansTable, {
          title: t('revenuePlansTitle'), rows: revenues, t, limit: LEDGER_LIMIT,
          onBackfill: id => setBackfill({ id, kind: 'revenue' }), backfillId: backfill?.kind === 'revenue' ? backfill.id : null,
          onBackfillSubmit: submitBackfill, onBackfillCancel: () => setBackfill(null),
          columns: [
            { label: t('org'), key: 'orgName' },
            { label: t('itemName'), key: 'itemName' },
            { label: t('month'), render: row => row.month ?? '—', num: true },
            { label: t('planned'), render: row => wan(row.plannedAmount) ?? '—', num: true },
            { label: t('actual'), render: row => wan(row.actualAmount) ?? '—', num: true },
            { label: t('variance'), render: row => wan(row.varianceAmount) ?? '—', num: true },
          ],
        }),
        h('section', { className: 'sf-panel' }, h('h2', null, t('prTitle')), h(Table, {
          columns: [
            { label: t('org'), key: 'orgName' },
            { label: 'PR', key: 'prCode' },
            { label: t('recordType'), render: row => row.recordType === 'SUBMITTED' ? t('prSubmitted') : t('prEstimated') },
            { label: t('description'), key: 'description' },
            { label: t('department'), key: 'departmentName' },
            { label: t('month'), render: row => row.month ?? '—', num: true },
            { label: t('amount'), render: row => wan(row.budgetAmount) ?? '—', num: true },
            { label: t('status'), key: 'status' },
          ],
          rows: ledger?.prRequests?.list || [],
          empty: t('empty'),
        }), h(LimitNote, { rows: ledger?.prRequests?.list, limit: LEDGER_LIMIT, t })))
    }

    // ---- 页面：资金 ----

    function Cash({ cash, t }) {
      const rows = cash?.data?.list || []
      return h('div', { className: 'sf-view' },
        h('section', { className: 'sf-panel' }, h(Table, {
          columns: [
            { label: t('org'), key: 'orgName' },
            { label: t('month'), render: row => `${row.year}-${String(row.month).padStart(2, '0')}`, num: true },
            { label: '期初余额', render: row => wan(row.prevMonthEndBalance) ?? '—', num: true },
            { label: '预计收入', render: row => wan(row.plannedIncome) ?? '—', num: true },
            { label: '预计支出', render: row => wan(row.plannedExpense) ?? '—', num: true },
            { label: '预计余额', render: row => h('span', { className: finite(row.projectedBalance) < 0 ? 'sf-neg' : '' }, wan(row.projectedBalance) ?? '—'), num: true },
            { label: '实际收入', render: row => wan(row.actualIncome) ?? '—', num: true },
            { label: '实际支出', render: row => wan(row.actualExpense) ?? '—', num: true },
            { label: '实际余额', render: row => h('span', { className: finite(row.actualBalance) < 0 ? 'sf-neg' : '' }, wan(row.actualBalance) ?? '—'), num: true },
          ],
          rows,
          empty: t('empty'),
        })))
    }

    // ---- 页面：预警 ----

    function Alerts({ alerts, t }) {
      const [busy, setBusy] = useState(false)
      const [notice, setNotice] = useState(null)
      const run = async () => {
        setBusy(true); setNotice(null)
        try {
          const result = await api('/alerts/run', { method: 'POST', body: '{}' })
          const data = result.data || {}
          setNotice(t('runDone').replace('{created}', data.created ?? 0).replace('{updated}', data.updated ?? 0).replace('{resolved}', data.resolved ?? 0))
          window.dispatchEvent(new CustomEvent('sf:refresh'))
        } catch (error) { setNotice(`${t('loadError')}: ${error.message}`) } finally { setBusy(false) }
      }
      const rows = alerts?.data?.list || []
      return h('div', { className: 'sf-view' },
        h('div', { className: 'sf-toolbar' },
          h('button', { type: 'button', className: 'sf-primary', disabled: busy, onClick: run }, busy ? t('running') : t('runEngine')),
          notice ? h('span', { className: 'sf-notice' }, notice) : null),
        h('section', { className: 'sf-panel' }, h('h2', null, t('alertsTitle')), h(Table, {
          columns: [
            { label: t('severity'), render: row => h(SeverityBadge, { severity: row.severity, t }) },
            { label: t('alertType'), key: 'alertType' },
            { label: t('title'), key: 'title' },
            { label: t('detail'), key: 'detail' },
            { label: t('org'), key: 'orgName' },
            { label: t('lastSeen'), render: row => shortDate(row.lastSeenAt) ?? '—' },
          ],
          rows,
          empty: t('empty'),
        })))
    }

    // ---- 页面：数据治理 ----

    function Governance({ quality, t }) {
      const issues = quality?.quality?.issues || []
      const batches = quality?.batches?.list || []
      return h('div', { className: 'sf-view' },
        h('section', { className: 'sf-panel' }, h('h2', null, t('qualityTitle')), h(Table, {
          columns: [
            { label: t('dataset'), key: 'dataset' },
            { label: t('issue'), key: 'label' },
            { label: t('count'), key: 'count', num: true },
            { label: t('severity'), render: row => h(SeverityBadge, { severity: row.severity === 'CRITICAL' ? 'CRITICAL' : row.severity === 'WARN' ? 'WARN' : 'INFO', t }) },
          ],
          rows: issues,
          empty: t('empty'),
        })),
        h('section', { className: 'sf-panel' }, h('h2', null, t('batchesTitle')), h(Table, {
          columns: [
            { label: t('dataset'), key: 'bizType' },
            { label: t('sourceFile'), key: 'sourceFile' },
            { label: t('rows'), key: 'totalRows', num: true },
            { label: t('successRows'), key: 'successRows', num: true },
            { label: t('failedRows'), key: 'failedRows', num: true },
            { label: t('batchStatus'), key: 'status' },
            { label: t('startedAt'), render: row => shortDate(row.startedAt) ?? '—' },
          ],
          rows: batches,
          empty: t('empty'),
        })))
    }

    // ---- 页面：数据录入 ----

    function Entry({ context, t }) {
      const [kind, setKind] = useState('payment')
      const [busy, setBusy] = useState(false)
      const [notice, setNotice] = useState(null)
      const orgOptions = (context?.data?.orgs || []).map(org => [org.id, org.name])
      const submit = async event => {
        event.preventDefault()
        const form = new FormData(event.currentTarget)
        const num = key => Number(form.get(key))
        const text = key => { const value = String(form.get(key) || '').trim(); return value || undefined }
        setBusy(true); setNotice(null)
        try {
          let path, payload
          if (kind === 'payment') {
            path = '/payment-plans'
            payload = { orgId: text('orgId'), planType: text('planType'), year: num('year'), description: text('description'), plannedAmount: num('plannedAmount') }
            if (text('planMonth')) payload.planMonth = num('planMonth')
            if (text('payeeName')) payload.payeeName = text('payeeName')
            if (text('costItemName')) payload.costItemName = text('costItemName')
          } else if (kind === 'revenue') {
            path = '/revenue-plans'
            payload = { orgId: text('orgId'), year: num('year'), itemName: text('itemName'), plannedAmount: num('plannedAmount') }
            if (text('month')) payload.month = num('month')
            if (text('payerName')) payload.payerName = text('payerName')
          } else {
            path = '/budget-lines'
            payload = { orgId: text('orgId'), year: num('year'), expenseType: text('expenseType'), budgetAmount: num('budgetAmount') }
            if (text('month')) payload.month = num('month')
            if (text('departmentName')) payload.departmentName = text('departmentName')
            if (text('costItemName')) payload.costItemName = text('costItemName')
            if (text('note')) payload.note = text('note')
          }
          await api(path, { method: 'POST', body: JSON.stringify(payload) })
          setNotice(t('submitted'))
          event.currentTarget.reset()
          window.dispatchEvent(new CustomEvent('sf:refresh'))
        } catch (error) { setNotice(`${t('loadError')}: ${error.message}`) } finally { setBusy(false) }
      }
      const years = yearOptions()
      return h('div', { className: 'sf-view' },
        h('p', { className: 'sf-panel-note' }, t('entryHint')),
        h('div', { className: 'sf-range-control' }, ...[['payment', 'entryPaymentPlan'], ['revenue', 'entryRevenuePlan'], ['budget', 'entryBudgetLine']].map(([id, key]) =>
          h('button', { type: 'button', key: id, className: kind === id ? 'is-active' : '', onClick: () => setKind(id) }, t(key)))),
        h('form', { className: 'sf-form', key: kind, onSubmit: submit },
          h(FormRow, { label: t('org') }, h(Select, { name: 'orgId', required: true, options: orgOptions, placeholder: t('org') })),
          h(FormRow, { label: t('year') }, h(Select, { name: 'year', required: true, options: years, placeholder: t('year') })),
          kind === 'payment' ? [
            h(FormRow, { key: 'pt', label: t('planType') }, h(Select, { name: 'planType', required: true, options: PLAN_TYPES })),
            h(FormRow, { key: 'pd', label: t('description') }, h(Field, { name: 'description', required: true, maxLength: 500 })),
            h(FormRow, { key: 'pm', label: t('planMonth') }, h(Field, { name: 'planMonth', type: 'number', min: 1, max: 12 })),
            h(FormRow, { key: 'py', label: t('payee') }, h(Field, { name: 'payeeName', maxLength: 200 })),
            h(FormRow, { key: 'pc', label: t('costItem') }, h(Field, { name: 'costItemName', maxLength: 100 })),
            h(FormRow, { key: 'pa', label: `${t('planned')}（${t('unitYuan')}）` }, h(Field, { name: 'plannedAmount', type: 'number', min: 0.01, step: '0.01', required: true })),
          ] : kind === 'revenue' ? [
            h(FormRow, { key: 'ri', label: t('itemName') }, h(Field, { name: 'itemName', required: true, maxLength: 200 })),
            h(FormRow, { key: 'rm', label: t('month') }, h(Field, { name: 'month', type: 'number', min: 1, max: 12 })),
            h(FormRow, { key: 'rp', label: t('payer') }, h(Field, { name: 'payerName', maxLength: 200 })),
            h(FormRow, { key: 'rt', label: t('incomeType') }, h(Field, { name: 'incomeTypeName', maxLength: 100 })),
            h(FormRow, { key: 'ra', label: `${t('planned')}（${t('unitYuan')}）` }, h(Field, { name: 'plannedAmount', type: 'number', min: 0.01, step: '0.01', required: true })),
          ] : [
            h(FormRow, { key: 'be', label: t('expenseType') }, h(Select, { name: 'expenseType', required: true, options: EXPENSE_TYPES })),
            h(FormRow, { key: 'bm', label: t('month') }, h(Field, { name: 'month', type: 'number', min: 1, max: 12 })),
            h(FormRow, { key: 'bd', label: t('department') }, h(Field, { name: 'departmentName', maxLength: 100 })),
            h(FormRow, { key: 'bc', label: t('costItem') }, h(Field, { name: 'costItemName', maxLength: 100 })),
            h(FormRow, { key: 'ba', label: `${t('kpiBudget')}（${t('unitYuan')}）` }, h(Field, { name: 'budgetAmount', type: 'number', min: 0.01, step: '0.01', required: true })),
            h(FormRow, { key: 'bn', label: t('note') }, h(Field, { name: 'note', maxLength: 500 })),
          ],
          h('div', { className: 'sf-form-actions' }, h('button', { type: 'submit', className: 'sf-primary', disabled: busy }, busy ? t('loading') : t('submit')), notice ? h('span', { className: 'sf-notice' }, notice) : null)))
    }

    // ---- 页面：深度分析入口 ----

    function sendToAgent(prompt, setNotice, t) {
      // 优先直接投递到当前会话；宿主未暴露 conversation 服务时降级为剪贴板复制。
      try {
        const conversation = globalThis.__sensteed_finance_conversation?.() ?? null
        if (conversation?.send) {
          void conversation.send(prompt).then(() => setNotice(t('sent'))).catch(() => copyFallback(prompt, setNotice, t))
          return
        }
      } catch { /* fallthrough */ }
      copyFallback(prompt, setNotice, t)
    }

    function copyFallback(prompt, setNotice, t) {
      const done = () => setNotice(t('sendFail'))
      try { navigator.clipboard?.writeText(prompt)?.then?.(done, done) } catch { done() }
    }

    function Analyze({ context, year, orgId, t }) {
      const [notice, setNotice] = useState(null)
      const orgName = (context?.data?.orgs || []).find(org => org.id === orgId)?.name
      return h('div', { className: 'sf-view' },
        h('p', { className: 'sf-panel-note' }, t('analyzeHint')),
        h('div', { className: 'sf-analysis-grid' }, ...ANALYSIS_ENTRIES.map(entry => h('article', { className: 'sf-panel sf-analysis-card', key: entry.id },
          h('div', { className: 'sf-analysis-head' }, h(Glyph, { name: entry.icon, size: 16 }), h('h2', null, t(`analysis_${entry.id}`))),
          h('p', null, t(`analysis_${entry.id}_desc`)),
          h('button', { type: 'button', className: 'sf-primary', onClick: () => sendToAgent(entry.prompt(year, orgName), setNotice, t) }, h(Glyph, { name: 'sparkle', size: 13 }), t('send'))))),
        notice ? h('p', { className: 'sf-notice' }, notice) : null,
        h('p', { className: 'sf-panel-note' }, t('analyzingNote')))
    }

    // ---- 外壳 ----

    function Dashboard({ t }) {
      const [tab, setTab] = useState('overview')
      const currentYear = new Date().getFullYear()
      const [year, setYear] = useState(String(currentYear))
      const [orgId, setOrgId] = useState('')
      const [revision, setRevision] = useState(0)
      const [context, setContext] = useState(null)
      const [blocks, setBlocks] = useState({})
      const [loading, setLoading] = useState(false)
      const [error, setError] = useState(false)
      const [partial, setPartial] = useState(false)
      const shellRef = useRef(null)
      const loadingRef = useRef(false)

      useEffect(() => {
        if (!opened) return undefined
        let cancelled = false
        void api('/context').then(value => { if (!cancelled) setContext(value) }).catch(() => {})
        return () => { cancelled = true }
      }, [opened, revision])

      useEffect(() => {
        if (!opened) { loadingRef.current = false; return undefined }
        loadingRef.current = true
        setLoading(true); setError(false); setPartial(false)
        const query = new URLSearchParams({ year })
        if (orgId) query.set('orgId', orgId)
        void (async () => {
          // 并行拉取全部数据块：任一块失败不拖慢其余块（原先串行瀑布 8 段往返）
          const settled = await Promise.allSettled([
            api(`/brief?${query}`),
            api(`/budget?${query}&limit=${BUDGET_LINE_LIMIT}`),
            api(`/pr?${query}&limit=${LEDGER_LIMIT}`),
            api(`/payment-plans?${query}&limit=${LEDGER_LIMIT}`),
            api(`/revenue-plans?${query}&limit=${LEDGER_LIMIT}`),
            api(`/cash?${query}`),
            api(`/alerts?year=${year}&status=OPEN&limit=100`),
            api('/quality'),
          ])
          if (cancelled) return
          const ok = result => result.status === 'fulfilled' ? result.value : null
          setBlocks({
            brief: ok(settled[0]),
            budget: ok(settled[1]),
            ledger: { prRequests: ok(settled[2])?.data ?? null, paymentPlans: ok(settled[3])?.data ?? null, revenuePlans: ok(settled[4])?.data ?? null },
            cash: ok(settled[5]),
            alerts: ok(settled[6]),
            quality: ok(settled[7]),
          })
          const failed = settled.filter(result => result.status === 'rejected').length
          setPartial(failed > 0 && failed < settled.length)
          setError(failed === settled.length)
          loadingRef.current = false
          setLoading(false)
        })()
        return () => { cancelled = true }
        // context 仅提供主体筛选项，不参与数据查询：不进依赖，避免主体列表到达时整页重复加载
      }, [opened, year, orgId, revision])

      // 统一的重载入口：sf:refresh 事件与界面按钮都经由它触发 revision 递增
      const refresh = () => setRevision(value => value + 1)
      useEffect(() => {
        if (!opened) return undefined
        window.addEventListener('sf:refresh', refresh)
        return () => window.removeEventListener('sf:refresh', refresh)
      }, [opened])

      useEffect(() => {
        if (!opened) return undefined
        const key = event => { if (event.key === 'Escape') closeOverlay() }
        window.addEventListener('keydown', key)
        return () => window.removeEventListener('keydown', key)
      }, [opened])

      // 打开时把焦点移入看板容器，保证键盘导航与 Escape 生效
      // 依赖只能是 opened：依赖数组在渲染期求值，引用未声明标识符会让看板首渲染即崩
      useEffect(() => { if (opened) requestAnimationFrame(() => shellRef.current?.focus?.()) }, [opened])

      const orgOptions = (context?.data?.orgs || []).map(org => [org.id, org.name])
      const years = yearOptions()
      const body = loading && !Object.keys(blocks).length
        ? h('div', { className: 'sf-loading', role: 'status' }, h(Glyph, { name: 'loading' }), t('loading'))
        : error && !Object.keys(blocks).length
          ? h('div', { className: 'sf-fatal', role: 'alert' }, h(Glyph, { name: 'warning' }), h('strong', null, t('loadError')), h('button', { type: 'button', onClick: refresh }, t('retry')))
          : h(React.Fragment, null,
            partial ? h('div', { className: 'sf-stale-notice', role: 'status' }, t('partialLoad')) : null,
            tab === 'overview' ? h(Overview, { brief: blocks.brief, t }) : null,
            tab === 'budget' ? h(Budget, { budget: blocks.budget, year, t }) : null,
            tab === 'ledger' ? h(Ledger, { ledger: blocks.ledger, t }) : null,
            tab === 'cash' ? h(Cash, { cash: blocks.cash, t }) : null,
            tab === 'alerts' ? h(Alerts, { alerts: blocks.alerts, t }) : null,
            tab === 'governance' ? h(Governance, { quality: blocks.quality, t }) : null,
            tab === 'entry' ? h(Entry, { context, t }) : null,
            tab === 'analyze' ? h(Analyze, { context, year, orgId, t }) : null)

      return h('div', { className: 'sf-overlay', role: 'dialog', 'aria-modal': true, 'aria-labelledby': 'sf-title' },
        h('main', { className: 'sf-shell', 'aria-labelledby': 'sf-title', ref: shellRef, tabIndex: -1 },
          h('header', { className: 'sf-header' },
            h('div', null, h('h1', { id: 'sf-title' }, t('title')), h('p', null, t('subtitle'))),
            h('div', { className: 'sf-header-buttons' },
              h(Tooltip, { label: t('refresh') }, h('button', { type: 'button', className: 'sf-icon-button', 'aria-label': t('refresh'), onClick: refresh }, h(IconRefreshOutline16, { size: 16 }))),
              h(Tooltip, { label: t('close') }, h('button', { type: 'button', className: 'sf-icon-button', 'aria-label': t('close'), onClick: closeOverlay }, h(IconCloseOutline16, { size: 16 }))))),
          h('div', { className: 'sf-toolbar' },
            h(FormRow, { label: t('year') }, h(Select, { value: year, onChange: setYear, options: years })),
            h(FormRow, { label: t('org') }, h(Select, { value: orgId, onChange: setOrgId, options: orgOptions, placeholder: t('allOrgs') }))),
          h('nav', { className: 'sf-tabs', 'aria-label': t('title') }, ...TABS.map(([id, key]) => h('button', { type: 'button', key: id, 'aria-current': tab === id ? 'page' : undefined, onClick: () => setTab(id) }, t(key)))),
          h('div', { className: `sf-content${loading ? ' sf-refreshing' : ''}`, 'aria-busy': loading }, body)))
    }

    function Button({ wide, t }) {
      return h(Tooltip, { label: t('open'), disabled: wide }, h('button', { type: 'button', className: `sf-button${wide ? ' sf-wide' : ''}`, 'aria-label': t('open'), onClick: openOverlay }, h(IconDataOutline16, { size: wide ? 14 : 18 }), wide ? h('span', null, t('open')) : null))
    }

    const css = `
    .sf-button{display:flex;width:36px;height:36px;align-items:center;justify-content:center;gap:8px;border:0;border-radius:6px;background:transparent;color:var(--dsw-alias-label-secondary);cursor:pointer}.sf-button:hover{background:var(--dsw-alias-bg-layer-2);color:var(--dsw-alias-label-primary)}.sf-wide{width:100%;height:34px;justify-content:flex-start;padding:0 10px}.sf-wide span{font-size:13px}
    .sf-overlay{position:fixed;inset:0;z-index:510;background:var(--dsw-alias-bg-base);color:var(--dsw-alias-label-primary)}.sf-shell{display:grid;grid-template-rows:auto auto auto 1fr;width:100%;height:100%;overflow:hidden}
    .sf-header{display:flex;min-height:72px;align-items:center;justify-content:space-between;gap:18px;padding:16px 24px;border-bottom:1px solid var(--dsw-alias-border-l1)}.sf-header h1{margin:0;font-size:20px;line-height:1.3}.sf-header p{margin:4px 0 0;color:var(--dsw-alias-label-secondary);font-size:13px}.sf-header-buttons{display:flex;gap:6px}
    .sf-icon-button{display:grid;width:36px;height:36px;place-items:center;border:1px solid var(--dsw-alias-border-l1);border-radius:6px;background:var(--dsw-alias-bg-layer-1);color:inherit;cursor:pointer}.sf-icon-button:hover{background:var(--dsw-alias-bg-layer-2)}
    .sf-toolbar{display:flex;align-items:center;gap:18px;padding:10px 28px;border-bottom:1px solid var(--dsw-alias-border-l1);flex-wrap:wrap}.sf-toolbar .sf-form-row{margin:0}
    .sf-tabs{display:flex;gap:2px;padding:0 28px;border-bottom:1px solid var(--dsw-alias-border-l1);overflow-x:auto}.sf-tabs button{height:42px;padding:0 14px;border:0;border-bottom:2px solid transparent;background:transparent;color:var(--dsw-alias-label-secondary);font:inherit;font-size:13px;white-space:nowrap;cursor:pointer}.sf-tabs button[aria-current]{border-bottom-color:var(--dsw-alias-brand-primary);color:var(--dsw-alias-label-primary);font-weight:650}
    .sf-content{overflow:auto;padding:22px 28px 44px}.sf-content.sf-refreshing{opacity:.62;transition:opacity .2s}
    .sf-view{display:grid;gap:20px;max-width:1440px;margin:0 auto}
    .sf-metrics{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));border-block:1px solid var(--dsw-alias-border-l1)}.sf-metric{display:grid;min-height:100px;align-content:center;gap:7px;padding:14px 16px;border-right:1px solid var(--dsw-alias-border-l1)}.sf-metric:last-child{border-right:0}.sf-metric-label{display:flex;align-items:center;gap:6px;color:var(--dsw-alias-label-secondary);font-size:12px}.sf-metric strong{font-size:24px;line-height:1.1;font-variant-numeric:tabular-nums;overflow-wrap:anywhere}.sf-metric small{color:var(--dsw-alias-label-secondary);font-size:11px}.sf-tone-critical strong{color:color-mix(in srgb,var(--dsw-alias-state-error-primary) 60%,var(--dsw-alias-label-primary))}.sf-tone-warn strong{color:color-mix(in srgb,var(--dsw-alias-state-warn-primary) 60%,var(--dsw-alias-label-primary))}
    .sf-two-col{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:20px}
    .sf-panel{min-width:0;padding:16px;border:1px solid var(--dsw-alias-border-l1);border-radius:8px;background:var(--dsw-alias-bg-layer-1)}.sf-panel h2{margin:0 0 12px;font-size:14px;line-height:1.4}
    .sf-chart-wrap{display:grid;gap:12px}.sf-chart-legend{display:flex;justify-content:flex-end;gap:14px;color:var(--dsw-alias-label-secondary);font-size:11px}.sf-chart-legend span{display:inline-flex;align-items:center;gap:5px}.sf-legend-a{display:inline-block;width:14px;height:8px;background:var(--dsw-alias-brand-primary);opacity:.55}.sf-legend-b{display:inline-block;width:14px;height:8px;background:var(--dsw-alias-brand-primary);opacity:.2}
    .sf-chart{width:100%;height:auto;min-height:170px;overflow:visible}.sf-grid-line{stroke:var(--dsw-alias-border-l1);stroke-width:1}.sf-bar-a{fill:var(--dsw-alias-brand-primary);opacity:.6}.sf-bar-b{fill:var(--dsw-alias-brand-primary);opacity:.2}.sf-chart-label{fill:color-mix(in srgb,var(--dsw-alias-label-tertiary) 75%,var(--dsw-alias-label-primary));font-size:11px}
    .sf-table-scroll{max-width:100%;overflow-x:auto}.sf-table{width:100%;border-collapse:collapse;font-size:12px;font-variant-numeric:tabular-nums}.sf-table th,.sf-table td{padding:8px 8px;border-bottom:1px solid var(--dsw-alias-border-l1);text-align:right;white-space:nowrap}.sf-table th:first-child,.sf-table td:first-child{text-align:left}.sf-table th{color:var(--dsw-alias-label-secondary);font-weight:500;font-size:11px}.sf-num{font-variant-numeric:tabular-nums}
    .sf-empty,.sf-loading,.sf-fatal{display:grid;min-height:120px;place-items:center;align-content:center;gap:8px;color:var(--dsw-alias-label-secondary);font-size:13px;text-align:center}.sf-fatal button{min-height:32px;padding:0 12px;border:1px solid var(--dsw-alias-border-l2);border-radius:6px;background:var(--dsw-alias-bg-layer-1);color:inherit;font:inherit;cursor:pointer}
    .sf-badge{display:inline-flex;align-items:center;padding:2px 8px;border-radius:999px;background:var(--dsw-alias-bg-layer-2);font-size:11px}.sf-badge-critical{color:color-mix(in srgb,var(--dsw-alias-state-error-primary) 60%,var(--dsw-alias-label-primary))}.sf-badge-warn{color:color-mix(in srgb,var(--dsw-alias-state-warn-primary) 60%,var(--dsw-alias-label-primary))}.sf-badge-info{color:var(--dsw-alias-label-secondary)}
    .sf-bar-track{height:6px;border-radius:4px;background:var(--dsw-alias-bg-layer-2);overflow:hidden;min-width:80px}.sf-bar-fill{height:100%;border-radius:4px;background:var(--dsw-alias-brand-primary)}
    .sf-toolbar{display:flex;align-items:center;gap:14px}.sf-notice{color:var(--dsw-alias-label-secondary);font-size:12px}.sf-neg{color:color-mix(in srgb,var(--dsw-alias-state-error-primary) 65%,var(--dsw-alias-label-primary));font-weight:650}
    .sf-range-control{display:inline-flex;padding:2px;border:1px solid var(--dsw-alias-border-l1);border-radius:6px;background:var(--dsw-alias-bg-layer-1)}.sf-range-control button{min-width:88px;height:30px;padding:0 12px;border:0;border-radius:4px;background:transparent;color:var(--dsw-alias-label-secondary);font:inherit;font-size:12px;cursor:pointer}.sf-range-control button.is-active{background:var(--dsw-alias-label-primary);color:var(--dsw-alias-bg-base);font-weight:650}
    .sf-form{display:grid;gap:12px;max-width:560px}.sf-form-row{display:grid;grid-template-columns:150px minmax(0,1fr);align-items:center;gap:12px}.sf-form-row>span{color:var(--dsw-alias-label-secondary);font-size:12px}.sf-form-row input,.sf-form-row select{height:32px;padding:0 10px;border:1px solid var(--dsw-alias-border-l2);border-radius:6px;background:var(--dsw-alias-bg-layer-1);color:inherit;font:inherit;font-size:13px}.sf-form-actions{display:flex;align-items:center;gap:12px}
    .sf-primary{display:inline-flex;align-items:center;gap:6px;height:32px;padding:0 14px;border:0;border-radius:6px;background:var(--dsw-alias-brand-primary);color:var(--dsw-alias-label-primary-foreground);font:inherit;font-size:13px;font-weight:600;cursor:pointer}.sf-primary:disabled{opacity:.5;cursor:default}.sf-link{border:0;background:transparent;color:var(--dsw-alias-brand-primary);font:inherit;font-size:12px;cursor:pointer;text-decoration:underline}
    .sf-inline-form{display:grid;gap:10px;margin-bottom:14px;padding:12px;border:1px solid var(--dsw-alias-border-l2);border-radius:8px}
    .sf-analysis-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(300px,1fr));gap:16px}.sf-analysis-card{display:grid;gap:10px;align-content:start}.sf-analysis-head{display:flex;align-items:center;gap:8px}.sf-analysis-head h2{margin:0}.sf-analysis-card p{margin:0;color:var(--dsw-alias-label-secondary);font-size:12px;line-height:1.55}.sf-analysis-card button{justify-self:start}
    .sf-panel-note{margin:0;color:var(--dsw-alias-label-secondary);font-size:12px;line-height:1.55}
    .sf-stale-notice{padding:8px 14px;border-radius:6px;background:color-mix(in srgb,var(--dsw-alias-state-warn-primary) 10%,transparent);color:var(--dsw-alias-label-secondary);font-size:12px}
    @media(max-width:900px){.sf-header,.sf-toolbar,.sf-tabs{padding-left:16px;padding-right:16px}.sf-content{padding:16px}.sf-metrics{grid-template-columns:repeat(2,minmax(0,1fr))}.sf-two-col{grid-template-columns:1fr}.sf-form-row{grid-template-columns:110px minmax(0,1fr)}}
    `

    function apply(ctx) {
      ctx.effect(() => ctx.locale.register(NS, copy), 'dofe-sensteed-finance: dictionaries')
      ctx.effect(() => { window.addEventListener(OVERLAY_EVENT, closeOtherOverlay); return () => window.removeEventListener(OVERLAY_EVENT, closeOtherOverlay) }, 'dofe-sensteed-finance: exclusive-overlay')
      ctx.effect(() => { const style = document.createElement('style'); style.dataset.plugin = '@dofe/dsh-sensteed-finance'; style.textContent = css; document.head.appendChild(style); return () => style.remove() }, 'dofe-sensteed-finance: styles')
      // 深度分析入口走宿主 conversation 服务（由 desktop 宿主在浏览器侧注册）；
      // 通过惰性 getter 注入，避免插件客户端与宿主模块产生编译期耦合。
      try {
        const conversation = ctx.get?.('conversation')
        globalThis.__sensteed_finance_conversation = conversation ? () => conversation : undefined
      } catch { globalThis.__sensteed_finance_conversation = undefined }
      const t = ctx.locale.bind(NS)
      ctx.slots.inject('sidebar.footer.action', () => ctx.slots.register({ name: 'sidebar.footer.action', id: 'dofe-sensteed-finance', order: 56, inject: () => ({ t }) }, Button))
      ctx.slots.inject('shell.overlay', () => ctx.slots.register({ name: 'shell.overlay', id: 'dofe-sensteed-finance', order: 56, inject: () => ({ t }) }, Overlay))
    }

    function Overlay({ t }) {
      const visible = useSyncExternalStore(subscribe, snapshot, snapshot)
      return visible ? h(Dashboard, { t }) : null
    }

    module.exports = { apply, inject: ['slots', 'locale'] }

    exports.apply = apply; exports.inject = ['slots', 'locale']; return module.exports; },
});
