- insert:
    - id: mcp-tools-platform
      name: '@deepseek-ai/dsh-mcp-client'
      config:
        transport: streamable-http
        serverName: tools-platform
        url: 'https://ixicai.cn/mcp/tools/platform'
        headers:
          Authorization: !!js ('Bearer ' + (process.env.MODELS_API_KEY || ''))
        toolCallTimeoutMs: 60000
        failOnStartupError: false
        reconnect:
          enabled: true

    - id: mcp-tools-supply-chain
      name: '@deepseek-ai/dsh-mcp-client'
      config:
        transport: streamable-http
        serverName: tools-supply-chain
        url: 'https://ixicai.cn/mcp/tools/supply-chain'
        headers:
          Authorization: !!js ('Bearer ' + (process.env.MODELS_API_KEY || ''))
        toolCallTimeoutMs: 60000
        failOnStartupError: false
        reconnect:
          enabled: true

    - id: mcp-tools-talent-discovery
      name: '@deepseek-ai/dsh-mcp-client'
      config:
        transport: streamable-http
        serverName: tools-talent-discovery
        url: 'https://ixicai.cn/mcp/tools/talent-discovery'
        headers:
          Authorization: !!js ('Bearer ' + (process.env.MODELS_API_KEY || ''))
        toolCallTimeoutMs: 60000
        failOnStartupError: false
        reconnect:
          enabled: true

    - id: mcp-tools-lead-discovery
      name: '@deepseek-ai/dsh-mcp-client'
      config:
        transport: streamable-http
        serverName: tools-lead-discovery
        url: 'https://ixicai.cn/mcp/tools/lead-discovery'
        headers:
          Authorization: !!js ('Bearer ' + (process.env.MODELS_API_KEY || ''))
        toolCallTimeoutMs: 60000
        failOnStartupError: false
        reconnect:
          enabled: true

    - id: mcp-tools-lead-monitor
      name: '@deepseek-ai/dsh-mcp-client'
      config:
        transport: streamable-http
        serverName: tools-lead-monitor
        url: 'https://ixicai.cn/mcp/tools/lead-monitor'
        headers:
          Authorization: !!js ('Bearer ' + (process.env.MODELS_API_KEY || ''))
        toolCallTimeoutMs: 60000
        failOnStartupError: false
        reconnect:
          enabled: true

    - id: mcp-tools-hotspot-discovery
      name: '@deepseek-ai/dsh-mcp-client'
      config:
        transport: streamable-http
        serverName: tools-hotspot-discovery
        url: 'https://ixicai.cn/mcp/tools/hotspot-discovery'
        headers:
          Authorization: !!js ('Bearer ' + (process.env.MODELS_API_KEY || ''))
        toolCallTimeoutMs: 60000
        failOnStartupError: false
        reconnect:
          enabled: true

    - id: mcp-tools-custom-car-monitoring
      name: '@deepseek-ai/dsh-mcp-client'
      config:
        transport: streamable-http
        serverName: tools-custom-car-monitoring
        url: 'https://ixicai.cn/mcp/tools/custom-car-monitoring'
        headers:
          Authorization: !!js ('Bearer ' + (process.env.MODELS_API_KEY || ''))
        toolCallTimeoutMs: 60000
        failOnStartupError: false
        reconnect:
          enabled: true

    - id: mcp-tools-viral-video
      name: '@deepseek-ai/dsh-mcp-client'
      config:
        transport: streamable-http
        serverName: tools-viral-video
        url: 'https://ixicai.cn/mcp/tools/viral-video'
        headers:
          Authorization: !!js ('Bearer ' + (process.env.MODELS_API_KEY || ''))
        toolCallTimeoutMs: 60000
        failOnStartupError: false
        reconnect:
          enabled: true

    - id: mcp-tools-browser-intelligence
      name: '@deepseek-ai/dsh-mcp-client'
      config:
        transport: streamable-http
        serverName: tools-browser-intelligence
        url: 'https://ixicai.cn/mcp/tools/browser-intelligence'
        headers:
          Authorization: !!js ('Bearer ' + (process.env.MODELS_API_KEY || ''))
        toolCallTimeoutMs: 60000
        failOnStartupError: false
        reconnect:
          enabled: true

    - id: tools-geo-guidance
      name: '@dofe/dsh-tools-mcp'
