diff --git a/lib/index.js b/lib/index.js
--- a/lib/index.js
+++ b/lib/index.js
@@ -1003,6 +1003,8 @@
 * @returns Detached ordered patches; this function does not update the Loader.
 */
 function readProfilePatches(binName, context, initialProfile) {
+	// Embedded launchers own their filtered bundle graph and final platform overlays.
+	if (context.readPatches !== void 0) return structuredClone(context.readPatches());
 	const profile = initialProfile ?? loadProfileDirectory(binName, context.dir, context.installAnchor, { userLayer: false });
 	const patches = structuredClone([
 		...profile.layers.flatMap((layer) => layer.patches),
diff --git a/lib/types/profile-context.d.ts b/lib/types/profile-context.d.ts
--- a/lib/types/profile-context.d.ts
+++ b/lib/types/profile-context.d.ts
@@ -8,6 +8,8 @@
 }
 /** Current profile facts; scheduling and mutation belong to their callers. */
 export interface ProfileContext {
+    /** Embedded launcher composition, shared by management operations and HMR. */
+    readonly readPatches?: () => readonly PatchOptions[];
     readonly name: string;
     /** Packaged applications supply their bundled runtime instead of a PATH executable. */
     readonly packageManager?: ProfilePnpmInvocation;
